### File managed with puppet ###
### Served by:        '<%= @puppet_agent_server %>'
### Module:           '<%= scope.to_hash['module_name'] %>'
### Template source:  'MODULES<%= template_source.gsub(Regexp.new("^#{Puppet::Node::Environment.current[:modulepath].gsub(':','|')}"),"") %>'

[tcpout]
defaultGroup = <%= @splunk_servergroup %>

[tcpout:<%= @splunk_servergroup %>]
disabled = false
server = <%= @splunk_serverlist.flatten.join(',') %>
